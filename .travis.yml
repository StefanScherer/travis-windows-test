os: windows
language: bash
services: docker

script:
  - docker version
  - docker images
  - choco install -y nodejs
  - node --version
  - npm install -g rebase-docker-image
  - rebase-docker-image -h
